<h1 class="banner">
  <%= @list.name %>
</h1>

<%= link_to 'Add a movie', new_list_bookmark_path(@list) %>

<!--
  <% @list.bookmarks.each do |bookmark| %>
    <h3><%= bookmark.movie.title%></h3>
  <%# <%= link_to bookmark.movie.title, movie path %>
    <p><em><%= bookmark.comment%></em></p>
    <%# this isn't working yet %>
    <%= link_to 'Delete this movie from list',
      bookmark_path(bookmark),
      data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
    %>
  <% end %>
-->

<%= link_to 'Back to all lists', root_path %>



<% @list.bookmarks.each do |bookmark| %>
  <div class="card" style="width: 18rem;">
    <img src= <%= bookmark.movie.poster_url %> class="card-img-top" alt="film-poster">
    <div class="card-body">
      <h5 class="card-title"><%= bookmark.movie.title%></h5>
      <p class="card-text"><%= bookmark.comment%></p>
      <%= link_to 'Delete this movie from list',
        bookmark_path(bookmark),
        data: {turbo_method: :delete, turbo_confirm: "Are you sure?"},
        class: "btn btn-primary"
      %>
    </div>
  </div>
<% end %>
