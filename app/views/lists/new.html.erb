<h1>Create a new list</h1>

<%= simple_form_for @list do |f| %>
  <%= f.input :name %>
  <%= f.submit %>
<% end %>

<%# want the user to be able to add an image for their list (and have that be the background) %>

<%= link_to 'Back to all lists', root_path %>
